'use client'

import MemoGame from '@/components/memo-game'

export default function TestPage() {
  return (
    <div className='fixed inset-4 bg-zinc-900/90'>
      <MemoGame props={{}} className='size-full' />
    </div>
  )
}
